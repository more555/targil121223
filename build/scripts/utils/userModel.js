export class User {
    constructor(userID, firstName, lastName, email, avatar, isSeller, company, donuts) {
        this.userID = userID;
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.avatar = avatar;
        this.isSeller = isSeller;
        this.company = company;
        this.donuts = donuts;
    }
}
